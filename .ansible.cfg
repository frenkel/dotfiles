[defaults]
inventory = /home/frank/Source/ansible/hosts
retry_files_enabled = False
remote_user = frank

[ssh_connection]
pipelining = True

[privilege_escalation]
become = True
become_user = root
become_method = doas
